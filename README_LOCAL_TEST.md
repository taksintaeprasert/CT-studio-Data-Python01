# 🧪 ทดลองใช้ระบบบน Local

## คู่มือฉบับย่อสำหรับการทดสอบระบบบนเครื่องตัวเอง

---

## ✅ สิ่งที่ต้องมี

1. ✅ Python 3.8+ ติดตั้งแล้ว
2. ✅ Git ติดตั้งแล้ว
3. ✅ ไฟล์ `keygg.json` (service account key)
4. ✅ Google Sheets ตั้งค่าเรียบร้อย

---

## 🚀 เริ่มต้นใช้งาน (5 นาที)

### Step 1: Clone/Pull โค้ด

```bash
# ถ้ายังไม่ clone
git clone <repository_url>
cd CT-studio-Data-Python01

# หรือถ้า clone แล้ว
cd CT-studio-Data-Python01
git pull origin claude/github-workflow-setup-zXQeD
```

### Step 2: ติดตั้ง Dependencies

```bash
# สร้าง virtual environment (แนะนำ)
python -m venv venv

# เปิดใช้งาน venv
# Windows:
venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate

# ติดตั้ง packages
pip install -r requirements.txt
```

### Step 3: ตรวจสอบไฟล์

ตรวจสอบว่ามีไฟล์เหล่านี้:
- ✅ `keygg.json` (ต้องมี!)
- ✅ `config.yaml`
- ✅ `app.py`
- ✅ `auth.py`

### Step 4: รันระบบ

```bash
streamlit run app.py
```

หรือ (ถ้าต้องการเปิดให้อุปกรณ์อื่นเข้าได้):

```bash
streamlit run app.py --server.address 0.0.0.0
```

### Step 5: เปิด Browser

- ระบบจะเปิด browser อัตโนมัติ
- หรือเปิด: http://localhost:8501
- จากอุปกรณ์อื่น: http://<IP>:8501

---

## 🔐 ทดสอบ Login

### บัญชีทดสอบ

**Admin:**
- Username: `admin`
- Password: `admin123`

**Sales:**
- Username: `sales1`
- Password: `sales123`

**Sales 2:**
- Username: `sales2`
- Password: `sales123`

### ทดสอบฟีเจอร์

1. ✅ Login สำเร็จ
2. ✅ ดูข้อมูลผู้ใช้ใน Sidebar
3. ✅ สร้าง Order ใหม่
4. ✅ แก้ไข Order
5. ✅ จัดการลูกค้า
6. ✅ ดู Dashboard (อาจไม่มีข้อมูล)
7. ✅ กรอก Marketing Data (ต้องสร้าง sheets ก่อน)
8. ✅ Logout

---

## 📱 ทดสอบบนมือถือ

### เชื่อมต่อมือถือ

1. **รันบน PC:**
   ```bash
   streamlit run app.py --server.address 0.0.0.0
   ```

2. **หา IP ของ PC:**
   ```bash
   # Windows:
   ipconfig
   # ดูที่ IPv4 Address

   # Mac/Linux:
   ifconfig
   # ดูที่ inet
   ```

3. **เปิดบนมือถือ:**
   - เชื่อมต่อ WiFi เดียวกับ PC
   - เปิด browser
   - ไปที่: `http://<PC_IP>:8501`
   - ตัวอย่าง: `http://192.168.1.100:8501`

### ทดสอบ UI บนมือถือ

- ✅ เปิด Sidebar ได้ (กดปุ่ม >)
- ✅ Login ได้
- ✅ ปุ่มขนาดใหญ่ขึ้น (50px)
- ✅ Input fields ขยาย
- ✅ Tabs ใช้งานได้
- ✅ สร้าง Order ได้ (แต่อาจต้อง scroll เยอะ)
- ✅ Logout ได้

---

## 🛠️ การตั้งค่าเพิ่มเติม

### เปลี่ยน Password

1. **Generate password hash:**
   ```bash
   python generate_passwords.py
   ```

2. **Copy hash ที่ได้**

3. **แก้ไข config.yaml:**
   ```yaml
   credentials:
     usernames:
       admin:
         password: YOUR_NEW_HASH_HERE
   ```

4. **รีสตาร์ท app** (Ctrl+C แล้วรันใหม่)

### เพิ่ม User ใหม่

1. **Generate password:**
   ```bash
   python -c "import bcrypt; print(bcrypt.hashpw(b'newpassword123', bcrypt.gensalt()).decode())"
   ```

2. **แก้ config.yaml:**
   ```yaml
   credentials:
     usernames:
       newuser:
         email: newuser@ctstudio.com
         name: New User Name
         password: HASHED_PASSWORD
         role: sales
   ```

3. **รีสตาร์ท app**

### สร้าง Sheets สำหรับ Dashboard

ถ้ายังไม่ได้สร้าง `payments`, `chats`, `ads_budget`:

1. เปิด Google Sheets "ct data"
2. สร้าง 3 sheets ใหม่ (ดูรายละเอียดใน SETUP_DASHBOARD.md)
3. รีสตาร์ท app

---

## 🐛 แก้ปัญหา

### ❌ ModuleNotFoundError

**ปัญหา:** ไม่พบ module

**แก้:**
```bash
pip install -r requirements.txt
```

### ❌ FileNotFoundError: keygg.json

**ปัญหา:** ไม่พบไฟล์ keygg.json

**แก้:**
1. ตรวจสอบว่าไฟล์ `keygg.json` อยู่ใน folder เดียวกับ `app.py`
2. ตรวจสอบชื่อไฟล์ถูกต้อง (ต้องเป็น `keygg.json` ไม่ใช่ `keygg.json.txt`)

### ❌ Google Sheets permission denied

**ปัญหา:** ไม่มีสิทธิ์เข้าถึง sheets

**แก้:**
1. เปิด `keygg.json` ดู `client_email`
2. เปิด Google Sheets "ct data"
3. Share ให้ service account email (จาก step 1)
4. ให้สิทธิ์ **Editor**

### ❌ Streamlit ไม่เปิด browser

**แก้:**
1. เปิด browser เอง
2. ไปที่: http://localhost:8501

### ❌ Port 8501 ถูกใช้แล้ว

**แก้:**
```bash
# ใช้ port อื่น
streamlit run app.py --server.port 8502
```

### ❌ Login ไม่ได้

**แก้:**
1. ตรวจสอบ username/password
2. ลองรีสตาร์ท app
3. ลองล้าง browser cache
4. ตรวจสอบ `config.yaml` ถูกต้อง

---

## 💡 Tips การทดสอบ

### 1. Hot Reload

Streamlit รองรับ auto-reload:
- แก้ไขโค้ด → Save
- Streamlit จะถาม "Rerun?" → กด **Always rerun**
- ไม่ต้องรีสตาร์ทเอง

### 2. Debug Mode

ดู logs ใน Terminal:
- แสดง errors
- แสดง warnings
- ช่วยแก้ปัญหา

### 3. Clear Cache

ถ้าข้อมูลไม่อัพเดท:
- กดปุ่ม "🔄 รีเฟรชข้อมูล" ในแอป
- หรือกด `C` ใน Terminal (clear cache)
- หรือกด `Ctrl+R` ใน Browser

### 4. Stop App

หยุด Streamlit:
- กด `Ctrl+C` ใน Terminal
- หรือปิด Terminal

---

## ✅ Checklist ก่อน Deploy

ทดสอบทุกอย่างบน Local ก่อน:

- [x] Login/Logout ได้
- [x] สร้าง Order ได้
- [x] แก้ไข Order ได้
- [x] ลบ Order ได้ (ระวังลบผิด!)
- [x] จัดการลูกค้าได้
- [x] Dashboard แสดงข้อมูล (ถ้าสร้าง sheets แล้ว)
- [x] Marketing Data บันทึกได้ (ถ้าสร้าง sheets แล้ว)
- [x] ทดสอบบนมือถือ (optional)

---

## 🚀 ขั้นตอนถัดไป

เมื่อทดสอบเสร็จแล้ว:

1. ✅ ทุกอย่างทำงานได้บน Local
2. ✅ ไปที่ **DEPLOY_GUIDE.md**
3. ✅ Deploy บน Streamlit Cloud
4. ✅ แจก URL ให้ทีม
5. ✅ อ่าน **USER_GUIDE.md** ร่วมกับทีม

---

## 📞 ต้องการความช่วยเหลือ?

- อ่านคู่มือ: DEPLOY_GUIDE.md, USER_GUIDE.md
- ตรวจสอบ logs ใน Terminal
- ลองแก้ตามส่วน "แก้ปัญหา"
- Google error message
- ถามใน Streamlit Community

---

**Happy Testing! 🎉**

---

**สร้างโดย:** Claude Code
**วันที่:** 2025-12-19
