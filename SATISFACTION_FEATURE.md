# ระบบประเมินความพึงพอใจของลูกค้า (Customer Satisfaction System)

## 📋 สรุปฟีเจอร์

ระบบประเมินความพึงพอใจของลูกค้าสำหรับร้าน CT Studio ที่ออกแบบมาเพื่อเก็บข้อมูลความพึงพอใจของลูกค้าหลังรับบริการ และแสดงคะแนนของช่างแต่ละคนแบบ Visualization

## ✨ คุณสมบัติหลัก

### 1. **แบบฟอร์มประเมินความพึงพอใจ** (`/satisfaction`)
- 🎨 UI แบบ Onboarding ที่สวยงาม มี Animation ที่นุ่มนวล
- 📱 Responsive Design ใช้งานได้ทั้ง Desktop และ Mobile
- 🌈 Progress Bar แสดงความคืบหน้าการกรอกแบบฟอร์ม

#### ขั้นตอนการกรอก (7 ขั้นตอน):
1. **เลือกช่างที่ทำบริการ** - แสดงรายชื่อช่างทั้งหมดที่กำลัง active
2. **เลือกบริการที่รับ** - เลือกจาก Brow, Lip, Inliner, Hair, Pink Area, Other (ถ้าเลือก Other สามารถระบุเพิ่มเติมได้)
3. **ระดับความเจ็บระหว่างทำ** - คะแนน 1-5 (1=เจ็บที่สุด, 5=ไม่เจ็บเลย)
4. **การให้บริการของช่าง** - คะแนน 1-5 (1=แย่มาก, 5=ดีเยี่ยม)
5. **ความพึงพอใจหลังทำ** - คะแนน 1-5
6. **การบริการหน้าร้าน** - คะแนน 1-5
7. **การตอบแชท** - คะแนน 1-5

### 2. **หน้า Artist Score** (`/artist-score`)
- 📊 แสดงคะแนนช่างทั้งหมดในรูปแบบกราฟและตาราง
- 🎯 สามารถ Filter ได้ตาม:
  - ช่าง (รายบุคคล หรือทั้งหมด)
  - หมวดบริการ (Brow, Lip, Inliner, Hair, Pink Area, Other, ทั้งหมด)
  - ช่วงวันที่ (Date Range)

#### Visualizations:
1. **Bar Chart**: แสดงคะแนนเฉลี่ยรวมของช่างทั้งหมด
2. **Radar Chart**: แสดงคะแนนรายละเอียดของช่างที่เลือก (แสดงเมื่อเลือกช่างเฉพาะ)
3. **Rankings**: จัดอันดับช่างตามคะแนนเฉลี่ย พร้อม Medal สำหรับ Top 3
   - 🥇 อันดับ 1: เหรียญทอง
   - 🥈 อันดับ 2: เหรียญเงิน
   - 🥉 อันดับ 3: เหรียญทองแดง

#### ข้อมูลที่แสดง:
- คะแนนเฉลี่ยรวม (Overall Average)
- คะแนนแยกตามหมวด (การให้บริการของช่าง, ผลลัพธ์, หน้าร้าน, แชท)
- คะแนนแยกตามประเภทบริการ (Brow, Lip, etc.)
- จำนวนรีวิวทั้งหมด

### 3. **ปุ่มในหน้า Sales**
- ปุ่ม "⭐ ประเมินความพึงพอใจ" ในหน้า Sales
- เปิดหน้าฟอร์มประเมินในแท็บใหม่

## 🗄️ Database Schema

### ตาราง `customer_satisfaction`
```sql
CREATE TABLE customer_satisfaction (
  id SERIAL PRIMARY KEY,
  artist_id INTEGER NOT NULL REFERENCES staff(id),
  service_category VARCHAR(50) NOT NULL,
  service_other TEXT,
  pain_level INTEGER CHECK (pain_level >= 1 AND pain_level <= 5),
  artist_service_quality INTEGER CHECK (artist_service_quality >= 1 AND artist_service_quality <= 5),
  result_satisfaction INTEGER CHECK (result_satisfaction >= 1 AND result_satisfaction <= 5),
  front_desk_service INTEGER CHECK (front_desk_service >= 1 AND front_desk_service <= 5),
  chat_response_quality INTEGER CHECK (chat_response_quality >= 1 AND chat_response_quality <= 5),
  submitted_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

## 🚀 การติดตั้ง

### 1. Run Database Migration

ก่อนใช้งาน ต้อง run migration script ใน Supabase SQL Editor:

```bash
# ไปที่ Supabase Dashboard > SQL Editor
# แล้ว copy & paste ไฟล์นี้และ run:
database/migration_v18_customer_satisfaction.sql
```

### 2. Deploy โค้ด

โค้ดได้ถูก push ไปยัง branch `claude/read-handoff-brief-YYzJo` แล้ว

```bash
git checkout claude/read-handoff-brief-YYzJo
git pull origin claude/read-handoff-brief-YYzJo
```

### 3. ตรวจสอบการทำงาน

1. เข้าหน้า Sales: `/sales`
2. คลิกปุ่ม "⭐ ประเมินความพึงพอใจ"
3. กรอกแบบฟอร์มทดสอบ
4. เข้าหน้า Artist Score: `/artist-score`
5. ดูคะแนนที่เพิ่งกรอก

## 📁 ไฟล์ที่สร้าง

```
database/
├── migration_v18_customer_satisfaction.sql   # Database schema

app/
├── api/
│   └── satisfaction/
│       └── route.ts                          # API routes (GET/POST)
├── (dashboard)/
    ├── satisfaction/
    │   └── page.tsx                          # แบบฟอร์มประเมิน
    ├── artist-score/
    │   └── page.tsx                          # หน้าแสดงคะแนนช่าง
    └── sales/
        └── page.tsx                          # แก้ไข: เพิ่มปุ่ม
```

## 🎨 การออกแบบ UI/UX

### แบบฟอร์มประเมิน
- **สี**: Gradient สีชมพู-ม่วง-น้ำเงิน
- **Animation**: Fade-in และ Scale transitions
- **Icons**: Emoji สำหรับแต่ละ rating option
- **Progress Bar**: แสดงความคืบหน้า % พร้อมสีไล่เฉด
- **Responsive**: ปรับขนาดอัตโนมัติบนทุก device

### หน้า Artist Score
- **Charts**: ใช้ Chart.js (Bar และ Radar)
- **Rankings**: Card-based design พร้อม medal indicators
- **Filters**: Dropdown และ Date Range Picker
- **Colors**:
  - อันดับ 1: สีทอง (yellow)
  - อันดับ 2: สีเงิน (gray)
  - อันดับ 3: สีทองแดง (amber)

## 📊 ตัวอย่างการใช้งาน

### ตัวอย่าง API Call

**POST /api/satisfaction**
```json
{
  "artist_id": 1,
  "service_category": "Brow",
  "service_other": null,
  "pain_level": 4,
  "artist_service_quality": 5,
  "result_satisfaction": 5,
  "front_desk_service": 5,
  "chat_response_quality": 5
}
```

**GET /api/satisfaction**
```
/api/satisfaction?artist_id=1&start_date=2024-01-01&end_date=2024-01-31&category=Brow
```

## 🔧 การปรับแต่ง

### เพิ่มหมวดบริการใหม่

แก้ไขใน `app/(dashboard)/satisfaction/page.tsx` และ `app/(dashboard)/artist-score/page.tsx`:

```typescript
const SERVICE_CATEGORIES = [
  { id: 'Brow', label: 'Brow', icon: '💫' },
  { id: 'Lip', label: 'Lip', icon: '💋' },
  // เพิ่มหมวดใหม่ที่นี่
  { id: 'NewCategory', label: 'New Category', icon: '✨' },
]
```

### ปรับคำถามหรือคะแนน

แก้ไข constants ใน `app/(dashboard)/satisfaction/page.tsx`:

```typescript
const QUALITY_RATINGS: RatingOption[] = [
  { value: 1, label: 'แย่มาก', emoji: '😞' },
  // ปรับแต่งตามต้องการ
]
```

## 🐛 Troubleshooting

### ปัญหา: ไม่พบตาราง customer_satisfaction
**วิธีแก้**: Run migration script ใน Supabase SQL Editor

### ปัญหา: ไม่เห็นช่างในแบบฟอร์ม
**วิธีแก้**: ตรวจสอบว่ามี staff ที่มี `role='artist'` และ `is_active=true` ในตาราง staff

### ปัญหา: Chart ไม่แสดง
**วิธีแก้**: ตรวจสอบว่ามีข้อมูล ratings ในฐานข้อมูลแล้ว และ Chart.js ถูก register ถูกต้อง

## 📝 License

Copyright © 2024 CT Studio. All rights reserved.
