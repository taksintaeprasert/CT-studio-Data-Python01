# Dashboard Setup Guide

## ภาพรวม
Dashboard และ Marketing Data เป็น feature ใหม่ที่เพิ่มเข้ามาสำหรับการวิเคราะห์ข้อมูลธุรกิจและการตลาด

## สิ่งที่ต้องทำก่อนใช้งาน

### 1. สร้าง Google Sheets ใหม่ 3 sheets

คุณต้องสร้าง 3 sheets ใหม่ใน Google Sheets "ct data" ของคุณ:

#### Sheet 1: `payments`
คอลัมน์ที่ต้องมี (ตามลำดับ):
1. `payment_id` - รหัสการชำระเงิน
2. `order_id` - รหัส order ที่เกี่ยวข้อง
3. `payment_date` - วันที่ชำระเงิน (ISO format: YYYY-MM-DD)
4. `amount` - จำนวนเงินที่ลูกค้าจ่าย
5. `net_amount` - จำนวนเงินสุทธิ (หลังหักค่าธรรมเนียม)
6. `payment_method` - วิธีการชำระ (cash, transfer, credit_card_3%)
7. `note` - หมายเหตุ

#### Sheet 2: `chats`
คอลัมน์ที่ต้องมี (ตามลำดับ):
1. `chat_date` - วันที่ (ISO format: YYYY-MM-DD)
2. `chat_count` - จำนวน chat/สนทนาที่เกิดขึ้นในวันนั้น
3. `note` - หมายเหตุ

#### Sheet 3: `ads_budget`
คอลัมน์ที่ต้องมี (ตามลำดับ):
1. `week_start_date` - วันที่เริ่มต้นสัปดาห์ (ISO format: YYYY-MM-DD)
2. `week_end_date` - วันที่สิ้นสุดสัปดาห์ (ISO format: YYYY-MM-DD)
3. `budget_amount` - งบโฆษณา (ตัวเลข)
4. `platform` - แพลตฟอร์ม (Facebook, Google, Instagram, TikTok, LINE, Other)
5. `note` - หมายเหตุ

### 2. อัพเดท `orders` sheet (ถ้ายังไม่ได้ทำ)

ตรวจสอบว่า orders sheet มีคอลัมน์เหล่านี้:
- `total_price` (เปลี่ยนชื่อจาก total_income ถ้ายังเป็นชื่อเก่า)
- `total_paid` (เพิ่มคอลัมน์ใหม่ที่ตำแหน่งสุดท้าย)

## คุณสมบัติใหม่

### 📊 Dashboard Tab
แสดงภาพรวมธุรกิจและการตลาด:

**Main Metrics:**
1. **Sales** - ยอดจองทั้งหมด (total_price ของ orders ที่สร้างในช่วงเวลา)
2. **Income** - เงินที่รับจริง (net_amount จาก payments ในช่วงเวลา)

**Performance Metrics:**
3. Order Count - จำนวน orders
4. Items Sold - จำนวนสินค้าที่ขาย (ไม่รวมของฟรี)
5. AOV - Average Order Value
6. Revenue per Customer - รายได้เฉลี่ยต่อลูกค้า

**Upsell Performance:**
7. Upsell Count - จำนวน upsell
8. Upsell Value - มูลค่า upsell
9. Upsell Rate - อัตรา upsell (%)
10. Orders with Upsell - จำนวน orders ที่มี upsell

**Marketing Metrics:**
11. Conversion Rate - อัตราการแปลงลูกค้า (Orders / Chats %)
12. ROAS - Return on Ad Spend (Income / Ad Spend)

**Channel Performance:**
- แสดงกราฟเปรียบเทียบ performance ของแต่ละช่องทาง
- ตารางข้อมูลแยกตามช่องทาง

**Features:**
- เลือกช่วงเวลาได้ (default = เดือนปัจจุบัน)
- Export ข้อมูลเป็น CSV

### 📈 Marketing Data Tab
กรอกข้อมูลสำหรับคำนวณ metrics:

**Tab 1: Chat Count**
- กรอกจำนวน chat รายวัน
- ดูประวัติ chat count
- แสดงรวม chats ทั้งหมด

**Tab 2: Ads Budget**
- กรอกงบโฆษณารายสัปดาห์
- เลือก platform (Facebook, Google, Instagram, TikTok, LINE, Other)
- ดูประวัติงบโฆษณา
- แสดงรวมงบแยกตาม platform

## วิธีใช้งาน

### การใช้งาน Dashboard
1. เปิด tab "📊 Dashboard"
2. เลือกช่วงเวลาที่ต้องการดู (default = เดือนปัจจุบัน)
3. ดูข้อมูล metrics ต่างๆ
4. สามารถ export ข้อมูลเป็น CSV ได้

### การกรอก Chat Count
1. เปิด tab "📈 Marketing Data"
2. เลือก sub-tab "💬 Chat Count"
3. เลือกวันที่
4. กรอกจำนวน chat
5. กด "💾 บันทึก Chat Count"

### การกรอก Ads Budget
1. เปิด tab "📈 Marketing Data"
2. เลือก sub-tab "💰 Ads Budget"
3. เลือกวันที่เริ่มต้นและสิ้นสุดสัปดาห์
4. กรอกงบโฆษณา
5. เลือก platform
6. กด "💾 บันทึก Ads Budget"

## หมายเหตุ

- ถ้ายังไม่ได้สร้าง sheets ใหม่ ระบบจะแสดงข้อความแจ้งเตือนให้สร้าง
- ข้อมูลจะถูก cache 5 นาที เพื่อเพิ่มความเร็ว
- สามารถกด "🔄 รีเฟรชข้อมูล" เพื่อโหลดข้อมูลใหม่ได้ทันที
- ระบบรองรับการอัพเดทข้อมูล - ถ้ากรอกวันที่/สัปดาห์ที่มีอยู่แล้ว จะเป็นการอัพเดทข้อมูลเดิม

## ไฟล์ที่เกี่ยวข้อง

- `marketing_data.py` - ฟังก์ชันคำนวณ metrics ทั้งหมด
- `dashboard_page.py` - หน้า Dashboard UI
- `marketing_data_page.py` - หน้ากรอกข้อมูล Marketing
- `sheets_helper.py` - ฟังก์ชันโหลดข้อมูลจาก sheets อย่างปลอดภัย
- `payments.py` - ระบบจัดการการชำระเงิน (สำหรับอนาคต)
- `app.py` - Main application (เพิ่ม 2 tabs ใหม่)

## สรุป Metrics

### Sales vs Income
- **Sales** = ยอดจองทั้งหมด (อาจยังไม่ได้รับเงิน)
- **Income** = เงินที่รับจริงๆ (หลังหักค่าธรรมเนียม)

### Conversion Rate
- คำนวณจาก: (Orders / Chats) × 100
- ยิ่งสูงยิ่งดี หมายถึงทีม sales แปลงลูกค้าได้ดี

### ROAS
- คำนวณจาก: Income / Ad Spend
- ยิ่งสูงยิ่งดี หมายถึงโฆษณาคุ้มค่า
- เช่น ROAS = 5.0 หมายถึงลงทุน 1 บาท ได้กลับมา 5 บาท

### Upsell Rate
- คำนวณจาก: (Orders ที่มี Upsell / Total Orders) × 100
- ยิ่งสูงยิ่งดี หมายถึงขายเพิ่มได้มาก

## ตัวอย่างข้อมูลที่ควรกรอก

### ตัวอย่าง Chat Count
```
วันที่: 2025-01-15
จำนวน Chat: 25
หมายเหตุ: วันธรรมดา, traffic ปกติ
```

### ตัวอย่าง Ads Budget
```
วันที่เริ่มต้น: 2025-01-08 (วันจันทร์)
วันที่สิ้นสุด: 2025-01-14 (วันอาทิตย์)
งบ: 5000
Platform: Facebook
หมายเหตุ: Campaign ปีใหม่
```

## การแก้ปัญหา

### ถ้า Dashboard ไม่แสดงข้อมูล
1. ตรวจสอบว่าสร้าง sheets ใหม่ครบทั้ง 3 sheets แล้ว
2. ตรวจสอบชื่อคอลัมน์ว่าถูกต้อง
3. กด "🔄 รีเฟรชข้อมูล"

### ถ้าเกิด Error
1. ตรวจสอบการเชื่อมต่อ Internet
2. ตรวจสอบว่า Google Sheets ยังอยู่
3. ตรวจสอบว่า service account ยังมีสิทธิ์เข้าถึง
4. กด Ctrl+R รีเฟรชหน้า

---

**สร้างโดย:** Claude Code
**วันที่:** 2025-12-19
